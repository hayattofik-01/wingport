import{_ as t,o,c as r,a2 as a}from"./chunks/framework.CHeM0PsO.js";const l=JSON.parse('{"title":"FAQ","description":"","frontmatter":{"title":"FAQ"},"headers":[],"relativePath":"faq.md","filePath":"faq.md"}'),s={name:"faq.md"};function n(i,e,p,d,c,u){return o(),r("div",null,[...e[0]||(e[0]=[a('<blockquote><p>🚧 Wingport is in active development. The API described here is the v0.1 design. Star the repo to follow along, or join the early access list.</p></blockquote><h1 id="faq" tabindex="-1">FAQ <a class="header-anchor" href="#faq" aria-label="Permalink to &quot;FAQ&quot;">​</a></h1><p><strong>Is Wingport production-ready?</strong> Not yet — and we&#39;ll never let the docs pretend otherwise. v0.1 is an alpha: the core loop works end to end (deploy, authenticate, stream, meter, enforce quotas), the streaming transport is the basic version, and the API may change before 1.0. It&#39;s right for side projects, internal tools, and MVPs today; the hardening roadmap is public in the issues. When something isn&#39;t ready, the docs say so with a <span class="VPBadge warning">Planned</span> badge.</p><p><strong>What does it cost?</strong> Nothing. MIT licensed, self-hosted in your own Supabase project, free forever — including commercially. The self-hosted core will never be feature-gated. (A hosted convenience tier is on the long-term roadmap for teams that want it; it will run the same open-source code.)</p><p><strong>Do my prompts or keys ever touch Wingport&#39;s servers?</strong> No — there are no Wingport servers in your request path. The gateway runs in your Supabase project; keys live in your Supabase secrets; requests go app → your project → provider. If Wingport-the-project disappeared tomorrow, your deployment keeps working.</p><p><strong>How is this different from Firebase AI Logic / OpenRouter / writing my own edge function?</strong> Short version: Firebase AI Logic is Gemini-and-Google-only; OpenRouter and LiteLLM are server-side routers with no concept of your end users and (for OpenRouter) a hosted middleman in your path; a hand-rolled edge function is a backend you now maintain, minus the per-user limits, metering, and the mobile streaming client. The full honest breakdown is on the <a href="/comparison">Comparison</a> page.</p><p><strong>Can I use it with Firebase instead of Supabase?</strong> Not yet — Supabase is the v0.1 target. A Firebase deployment target (Cloud Functions + Firebase Auth verification) is on the roadmap. The Dart SDK is already auth-agnostic via the <code>tokenProvider</code> constructor.</p><p><strong>Does it work with Riverpod / Bloc / Provider?</strong> Yes — the SDK is state-management-agnostic. <code>Wingport</code> is a plain class, cheap to construct, safe as a singleton; <code>stream()</code> returns a standard Dart <code>Stream</code>. Wire it into whatever you already use.</p><p><strong>What stops one of my users from running up my AI bill?</strong> Per-user limits, enforced in the gateway before any tokens are spent: requests per minute (burst), requests per day, tokens per day, and concurrent-stream caps — with tiers mapped from your own user data. See <a href="/concepts/usage-quotas">Usage &amp; Quotas</a>.</p><p><strong>Which models can my app call?</strong> Only the aliases you list in <code>wingport.config.ts</code>. The client can&#39;t request anything you didn&#39;t configure — and because aliases map to concrete model IDs server-side, you can upgrade models with a redeploy and <strong>no app-store review</strong>.</p><p><strong>Why TypeScript for the gateway if this is a Dart project?</strong> Because Supabase Edge Functions run Deno. The Dart side is where your app lives (the SDK); the gateway is ~infrastructure you deploy once and rarely touch. A pure-Dart self-hosted gateway (Shelf) is on the roadmap for teams running Dart on the server.</p><p><strong>Can I contribute?</strong> Please. The project is young and built in public — the <a href="/reference/roadmap">roadmap</a> is real, the issues are scoped, and <code>good first issue</code> means it. Start with a GitHub Discussion about the AI feature you&#39;re building; real apps&#39; needs set the priority order.</p><hr>',13)])])}const g=t(s,[["render",n]]);export{l as __pageData,g as default};
